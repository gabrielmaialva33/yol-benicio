// Re-exports removed to avoid barrel file lint warnings
// Import components, hooks, pages, and types directly from their individual files when needed

// Components available in ./components/:
// - FolderDetailForm, FolderDetailHeader, FolderDetailPage
// - FolderDetailSidebar, FolderFilters, FolderPageLayout
// - FolderTable, FolderTabs, Pagination, ProcessTimeline

// Hooks available in ./hooks/:
// - useFolderConsultation (./hooks/use-folder-consultation)
// - useFolderDetail (./hooks/use-folder-detail)
// - useCreateFolder, useDeleteFolder, useFolder, etc. (./hooks/use-folders-api)

// Pages available in ./pages/:
// - FolderConsultationPage, FolderRegisterPage

// Types available in ./types/:
// - FolderConsultationResponse, FolderDetail, FolderDocument, etc.
