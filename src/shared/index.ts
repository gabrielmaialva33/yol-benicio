// Re-exports removed to avoid barrel file lint warnings
// Import functions, components, hooks, and types directly from their individual files when needed

// API functions available in ./api/auth:
// - login, logout, getMe, getStoredToken, setStoredToken, etc.
// - AuthInput, AuthState types

// Components available in ./components/:
// - Breadcrumb, ErrorBoundary, LoadingSkeleton, withErrorBoundary

// Hooks available in ./hooks/:
// - useAuth (./hooks/use-auth-hook)
// - AuthProvider (./hooks/use-auth)
// - AuthContext, AuthContextValue (./hooks/auth-context)
// - createApiHooks (./hooks/use-api)
// - useTasks (./hooks/use-tasks)

// Types available in ./types/api:
// - ApiResponse, PaginatedResponse, PaginationMeta, QueryParams, Timestamps

// Utilities available in ./utils/:
// - formatDateRange, generateAvatar, useDetectOutsideClick, useMediaQuery
